# iOS Calculator Web Application

### Requirements
- Node version >= 14.x
- yarn or npm

### Installation
- `cd calculator-ios`
- `yarn install`
- run dev server with `yarn start`
- open `http://localhost:8000`
